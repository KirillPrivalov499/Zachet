<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gorun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".NewStory.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewStory.RegistrationActivity"></activity>
        <activity android:name=".NewStory.ListPeopleActivity" />
        <activity android:name=".NewStory.SinginActivity"></activity>
        <activity android:name=".NewStory.ListMessages2Activity"
            android:parentActivityName=".NewStory.ListPeopleActivity"/>
        <activity android:name=".ChatStructure.DialogListsActivity" />
        <activity android:name=".ChatTutorial.MainActivity" />
    </application>

</manifest>